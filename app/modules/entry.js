define([
    "app"

], function( App ) {

    var Entry = App.module();

    Entry.Model = Backbone.Model.extend({
        defaults: {
        },

        initialize: function() {
        }
    });

    return Entry;
});
