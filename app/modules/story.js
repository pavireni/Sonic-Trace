define([
    "app"

], function( App ) {

    var Story = App.module();

    Story.Model = Backbone.Model.extend({
        defaults: {
        },

        initialize: function() {
        }
    });

    return Story;
});
